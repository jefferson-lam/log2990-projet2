import { KeyboardListenerDirective } from './keyboard-listener.directive';

describe('KeyboardListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardListenerDirective();
    expect(directive).toBeTruthy();
  });
});
